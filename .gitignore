# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtual environments
venv/
env/
ENV/

# MLflow local store (use Model Registry / cloud backend in CI)
mlruns/
mlartifacts/

# Trained model binaries (regenerable; tracked in MLflow Model Registry)
models/*.pkl
models/best_model_info.json

# Dataset (Kaggle license — download separately)
data/WA_Fn-UseC_-Telco-Customer-Churn.csv

# Heavy regenerable Evidently HTML reports
artifacts/drift_reports/*.html

# Runtime logs
*.log
mlflow_server.log
api_server.log

# IDE / OS
.idea/
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# Playwright MCP cache
.playwright-mcp/

# Runtime traffic-generator drift summaries (we only keep the canonical 5)
artifacts/drift_reports/batch_5_summary.json
artifacts/drift_reports/batch_6_summary.json
artifacts/drift_reports/batch_7_summary.json
artifacts/drift_reports/batch_8_summary.json
artifacts/drift_reports/batch_9_summary.json
artifacts/drift_reports/batch_1[0-9]_summary.json
artifacts/drift_reports/batch_2[0-9]_summary.json
artifacts/drift_reports/batch_3[0-9]_summary.json
artifacts/drift_reports/batch_4[0-9]_summary.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
branch_structure.json
